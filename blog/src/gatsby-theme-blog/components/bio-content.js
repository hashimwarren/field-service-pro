import React, { Fragment } from "react"
import { Styled } from "theme-ui"

/**
 * Change the content to add your own bio
 */

export default () => (
  <Fragment>
    Field Service Pro is job ticket manager for field technicians, a JavaScript/NodeJS/MongoDB app by Hashim Warren.
    
  </Fragment>
)
